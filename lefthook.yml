pre-commit:
  commands:
    check:
      glob: '*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}'
      run: pnpm lint {staged_files} --fix
commit-msg:
  commands:
    lint commit message:
      run: pnpm commitLint {1}
      stage_fixed: true
